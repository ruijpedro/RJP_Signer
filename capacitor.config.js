/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'pt.rjp.signer',
  appName: 'RJP Signer',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
